# G2 版本和差异记录
> 文档编号：G2-VERSION-DIFF-001
> 文档版本：v1.0
> 文档类型：governance
> 关联对象：PROJECT mindmate-ai
> 状态：active
> 关联来源：SRC-002、SRC-003、SRC-004、SRC-005、SRC-006、SRC-007

## 资料版本

- 产品说明书：V1.0，当前仓库首次登记。
- 项目需求文档：V1.0，当前仓库首次登记。
- Figma：文件名未标注项目版本；使用 SHA-256 作为本轮证据。
- Sketch：meta version 143；使用 SHA-256 作为本轮证据。
- README：当前仓库版本，无独立产品版本号。

未发现同一资料的历史副本或仓库内的旧版本差异，因此不能推断业务资料已经完成版本演进。

## 第一轮到第二轮的治理校准

1. HIGH-02 从“外部 Provider + 服务端适配层”的方向，进一步明确为外部模型/Embedding、用户本地后端、Provider Adapter、本地向量存储、本地部署、不需要云服务器。
2. HIGH-04 明确设置页和独立错题本延后，解决产品说明书和需求表之间的首版范围差异。
3. MEDIUM-02 将文本型四格式、OCR 关闭、20MB 和单次 5 文件固化为 V1 当前决策约束。
4. MEDIUM-04 只固定 REST/OpenAPI 的协议方向，明确路径、字段、鉴权和流式协议不提前确定。
5. MEDIUM-03 将测试范围收敛为单元、API/契约、核心 E2E、基础安全和轻量性能基线。
6. MEDIUM-01 和 LOW-02 明确模板为参考，不继承原品牌、文案和颜色作为最终 UI 事实。

## 变更性质

以上是项目决策层的校准，不是对原始 DOCX、Figma、Sketch 或 README 的内容修改。原始资料保持原样，后续 PRD 需要通过来源和决策引用这些结论。

