# 第四十五批：首页继续学习入口

- 日期：2026-09-26
- 分支：`feat/v1-bootstrap`
- 进场 SHA：本地与 `origin/feat/v1-bootstrap` 均为 `c5a39be40e1e6095826c580a0c6a423be4f92cf2`，工作区干净。
- 本批首页继续学习切片：`PASS`
- 阶段 8：`PARTIAL`
- 求职 Demo：维持第四十二批 `PASS`。本批没有重跑空库上传、建库和全链路问答。
- 真实 DeepSeek：`PENDING`。学习出题标识为 `learning-demo-fixture-v1`，`live_model_called=false`。没有 Key，没有外发。

## 交付

- 首页增加“继续学习”区域，读取已有 `GET /api/v1/history/learning-sessions?limit=1`。按最近活动时间只展示一条真实记录。
- 卡片显示主题、知识库名和范围内文件数、实际会话状态、已作答数量和最近学习时间。不显示掌握度、分数或复习日期。
- 未作答且来源有效时，主按钮是“继续学习”，地址为原 `/learning/session/:id`。已作答时主按钮是“查看反馈”。现有单题流程没有总结页，所以不显示“查看总结”。
- 来源失效时写明具体原因。未作答只提供“查看原会话”和“学习历史”，不提供“继续学习”。已作答保留“查看反馈”。首页点击只导航，不提交答案。
- 读取失败时不保留上一张卡片，显示“重新加载”和“打开学习历史”。
- 没有学习记录时，“开始第一次学习”进入知识库列表，“先导入资料”进入文件页。文案说明必须先选择索引就绪且有可用文件的知识库，不会用空白 ID 直接开题。
- 侧栏、历史记录和学习页原有入口保持可用。没有改后端、OpenAPI 或数据库。

## 自动化

- `npx vitest run src/test/stage8-home-learning.test.tsx`：`5 passed`。覆盖空引导、未作答原地址、已作答反馈、来源失效和读取失败后不显示旧卡片。五种状态都没有学习会话或 Attempt 的 POST。
- `npm run typecheck`、`npm run lint`、`npm run build` 通过。
- 没有改后端，因此没有重跑 pytest、Ruff、Pyright 或 OpenAPI 生成。没有重跑全套前端测试。切块 Worker 领取时序问题本批没有碰到，也不算已修复。

## 浏览器与重启

- 数据根：`%TEMP%\mindmate-ai-stage45-home-learning`。准备脚本使用 `docs/test-data/stage5-fixed-ready/` 和已校验 ONNX，没有下载模型。主库 `01a0de2b-b93e-7ab4-af7d-3ef505c7e667`，活动索引 `01a0de2b-c4b7-7801-b32f-4baf7d70f9d2`，状态 `READY`。
- 服务：`.\scripts\dev.ps1 -DataDir ... -ApiPort 8004 -WebPort 5177`。Provider 固定 Mock。
- 打开首页时还没有学习记录。页面给出“开始第一次学习”和“先导入资料”，并说明要先选索引就绪的知识库。随后从主库创建未作答会话 `01a0de2e-3d6e-7efa-a9f2-b361ee9d30c7`。首页读到同一会话 ID，状态“未作答”，已作答 `0 / 1`。点击“继续学习”回到原 URL，题目仍在，没有自动提交。
- 在原会话选择“30 秒”并提交一次。结果正确，引用 `[1] 服务超时策略.txt`，页面标明本地 Mock 演示。回到首页后状态是“已作答”，数量 `1 / 1`，按钮是“查看反馈”。刷新后卡片不变。Attempt `01a0de2f-8ef2-7858-bbb0-d094e0d1e1ff`，题目 `01a0de2e-4096-7412-aa71-31726dc3a007`。
- 重启：监听 PID `4804` 停止后，脚本退出码 0，8004/5177 已释放。同一数据根再次启动，新监听 PID `28280`。首页仍是同一会话和“已作答 1 / 1”，Attempt 仍是这一条。
- 另建会话 `01a0de33-28ea-76c8-a117-5ade18ec2479`。它在出题时就是 `FAILED`，证据门控 `BODY_ANCHOR_COVERAGE_LOW`，没有题目，也没有 Attempt。首页当时显示“未能出题”，没有“继续学习”。
- 文件 `01a0de2b-ba3d-7163-8c6d-735dfb0913c8`（服务超时策略.txt）进入回收站后，首页最新卡片写明“不能继续作答：文件已在回收站”。已作答会话保留“查看反馈”，打开后仍是“结果：正确”。未作答失败会话只有“查看原会话”和“学习历史”。学习历史两条都标为“资料范围已失效 · 文件已在回收站”。验证结束时 Attempt 仍是 1 条，题目仍是 1 道。
- 验证后停止本次启动的 8004/5177。

## 未纳入本批

- 首页真实统计、快捷操作、最近文件、最近知识库、最近对话和任务摘要。
- 历史搜索、筛选、批量删除、设置、备份恢复、诊断与清理。
- 第四十二批网页上传全链路没有重跑。
- 学习出题仍是本地规则 `learning-demo-fixture-v1`，不是在线模型生成。
