# 第四十六批：首页四个快捷操作

- 日期：2026-09-26
- 分支：`feat/v1-bootstrap`
- 进场 SHA：本地与 `origin/feat/v1-bootstrap` 均为 `dd504908156e7725c6ace2060f3710019b37759d`，工作区干净。
- 本批四个快捷操作切片：`PASS`
- 阶段 8：`PARTIAL`
- 求职 Demo：维持第四十二批 `PASS`。本批没有重跑空库上传、建库和全链路问答。
- 真实 DeepSeek：`PENDING`。聊天页仍显示 Mock 生成提示。没有 Key，没有外发。

## 交付

- 首页在“继续学习”之后固定四个入口：开始学习、AI 提问、导入文件、创建知识库。没有新增导航或组件库。
- 开始学习打开已有 `/learning/new`。没有指定知识库时只列出 `READY` 且有可用文件的知识库；没有这样的知识库时说明先导入并完成索引，并给出文件页和知识库页入口。选择知识库后进入原有一题准备表单，提交前不创建会话或题目。
- AI 提问打开已有 `/chat`。新对话可以选择普通对话，或选择索引就绪的知识库进入 `/chat?knowledge_base_id=`。没有就绪知识库时保留普通对话，并说明带引用问答需要先准备资料。打开页面和切换范围都不发送消息，也不建立会话。
- 导入文件打开已有文件页。原有导入是页内文件选择控件，不能由路由直接弹出，因此到达后保留可见的“导入文件”按钮。本批没有选择文件。
- 创建知识库打开已有 `/knowledge-bases/new`。名称可填写，提交按钮在有名称后可用；取消返回知识库列表，不创建知识库。
- 四个入口不因未配置外部 Provider 而消失。没有改后端、OpenAPI 或数据库。

## 自动化

- `npx vitest run src/test/stage8-home-shortcuts.test.tsx`：`3 passed`。覆盖空库四个落点、READY 知识库的学习和聊天范围、列表失败提示。这些路径没有文件、知识库、对话、学习会话或 Attempt 的写请求。
- 同轮还通过了首页继续学习、学习准备和 Provider 聊天相关 Vitest，共 `20 passed`。
- `npm run typecheck`、`npm run lint`、`npm run build` 通过。
- 没有改后端，因此没有重跑 pytest、Ruff、Pyright 或 OpenAPI 生成。没有重跑全套前端测试。切块 Worker 领取时序问题本批没有碰到，也不算已修复。

## 浏览器

- 空库数据根：`%TEMP%\mindmate-ai-stage46-home-shortcuts-empty`。服务 `.\scripts\dev.ps1 -DataDir ... -ApiPort 8006 -WebPort 5180`，Provider 固定 Mock。
- 空库首页同时有“继续学习”和四个快捷操作。开始学习说明还没有索引就绪的知识库，并给出导入文件和知识库入口。AI 提问是普通聊天，写明不会建立知识库会话，页面含 Mock 生成提示，发送按钮保持禁用。文件页能看到“导入文件”按钮。创建知识库表单填入“阶段46未提交”后提交按钮变为可用，随后点取消，知识库列表仍是空的。回到首页后继续学习仍是空引导。
- READY 数据根：把 `%TEMP%\mindmate-ai-stage5-fixed-ready` 复制到 `%TEMP%\mindmate-ai-stage46-home-shortcuts-ready`。服务端口 `8007/5181`。开始学习只列出两个 READY 库，未列出空的诊断库。进入“第二十五批·固定资料主库”后看到索引就绪、可用文件 2，成员含 `服务超时策略.txt`，“创建并开始”保持禁用。AI 提问可切到同一知识库，标题为知识库问答，范围名称与库名一致，消息框为空且未发送。
- 只点击和返回后，空库文件、知识库、对话、消息、学习会话、题目、Attempt 和任务都是 0。READY 副本保持文件 3、知识库 3、任务 13、对话 0、学习会话 0、题目 0、Attempt 0。
- 两套服务都以操作者停止结束，退出码 0。8006、5180、8007、5181 已释放。没有使用默认用户数据库，没有提交私人文件、Key、模型缓存或临时数据库。

## 未纳入本批

- 首页真实统计、最近文件、最近知识库、最近对话和任务摘要。
- 历史搜索、筛选、批量删除、设置、备份恢复、诊断与清理。
- 第四十二批网页上传全链路没有重跑。
- 学习出题仍是本地规则，不是在线模型生成。
