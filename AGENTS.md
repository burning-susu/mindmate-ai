# AGENTS.md
> 文档版本：V3.2-GENERIC
> 文档定位：项目 AI 运行宪章 / Runtime Entry
> 关联体系：通用文档提示词 v3.2
> 文档状态：通用基线

## 0. 文件定位

本文件是**项目级 AI 运行入口**，负责回答四件事：

1. 当前项目的基本身份与事实源在哪里；
2. AI 启动一次工作时先读取什么、如何判断当前场景；
3. 哪些规则默认生效，哪些规则需要 Capability / Workflow 激活；
4. AI 在什么情况下必须停下来确认，而不是自行扩大范围。

本文件不是完整开发手册，也不承担技术栈百科、测试方法论、UI SOP 等职责。具体知识放在 `doc/`，本文件只负责路由和硬约束。

## 1. 核心原则

- **先建立事实，再执行修改**：不知道当前项目事实时先读取对应来源，不用猜测补齐。
- **来源优先于习惯**：项目事实优先来自当前项目文档、源码、配置、接口契约和运行证据；通用规则只在事实不足时提供方法。
- **项目 Profile 决定路由**：技术栈、端侧、交付目标、规模、风险与平台限制由 `PROJECT_PROFILE` / `ASSESSMENT` 决定；不得把某一技术栈默认当成所有项目标准。
- **Workflow 与 Scale 分离**：项目规模不是唯一的流程选择器。高风险小项目可能需要增强治理；低风险大体量项目也不必机械加载全部能力。
- **Spec / Baseline 是业务事实源**：已经确认的业务范围、规则、验收口径以项目当前事实源为准；AI 不因“通常应该这样”而擅自改需求。
- **变更先治理、后编码**：涉及业务范围、架构、接口契约、数据结构、验收口径的变化，先进入变更链。
- **无证据不宣称完成**：代码写了不等于完成；测试通过不等于验收通过；合并不等于上线。
- **最小变更原则**：没有明确理由不得扩大修改范围，不做与当前目标无关的重构。

## 2. 事实源层级

按以下顺序解决冲突：

1. 已批准的项目/需求基线（Approved PRD、Spec、ADR 等）；
2. 当前源码、配置、数据库/接口契约等可验证事实；
3. 已记录且仍有效的 Decision / Change / Evidence；
4. `doc/governance` 中的治理规则；
5. `doc/rules` 中已激活的通用规则；
6. `doc/capabilities` 中按当前项目启用的专项能力；
7. AI 的一般经验。

若高层事实源与当前代码冲突，不得自动选择一方“看起来更合理”；应登记冲突并进入变更/校准流程。

## 2.1 V1 开发基线（2026-09-22）

当前项目已从历史 G0-G11 文档治理阶段切换到 V1 代码开发阶段：

- `PROJECT_STAGE=DEVELOPMENT`；当前代码开发输入是 `docs/project/requirements/v1/`。
- 当前需求事实源优先级为：`18_最终决策表.md` → `16_Codex开发任务书.md` → `00_需求规格总纲.md` → `01–15` 专题需求文件 → 当前代码与测试证据。
- `docs/archive/2026-09-22-pre-v1/` 只用于历史追溯，不参与当前产品、技术或实现决策。
- `项目业务资料/` 中的 Figma、Sketch 和 Word 原始资料必须保留，不修改、不删除。
- 当前已冻结的技术基线包括 React 19 + TypeScript + Vite、Python 3.12 + FastAPI、SQLite + FTS5 + sqlite-vec、本地 ONNX `BAAI/bge-small-zh-v1.5`、DeepSeek `deepseek-flash`、SQLite 持久任务、PyInstaller one-folder 和 Inno Setup。
- Provider Runtime 仍必须使用 Mock Provider 进行开发；真实 DeepSeek 请求、真实凭据和付费外部调用不属于日常自动化测试。
- 近期目标是先做成求职展示用的稳定 Demo：优先文件整理/导入、建库、带来源问答、最小学习陪练和重启恢复。完整 V1 的高级项与发布级验收稍后精进。报告必须分开写 `Demo 可用性` 和 `完整 V1 阶段状态`，不能用 Demo 通过冒充阶段 5/6 验收通过。
- 当前开发阶段不得重新发起产品、Provider、Embedding、向量存储、技术栈、页面结构或部署方式决策；普通实现细节按 15/16 的冻结方案处理。

## 3. 启动路由

每次新对话、重新接手任务、较大修改或发布前，先判断工作场景：

### 3.1 新项目
读取顺序：
`AGENTS.md` → `doc/governance/通用文档提示词v3.2.md` → `PROJECT_PROFILE / ASSESSMENT` → 当前已投放资料 → 对应 Capability。

### 3.2 新建需求 / 新 REQ
读取：
`AGENTS.md` → 当前 PROJECT_PROFILE / Workflow → 相关基线 → REQ 模板 → 相关 Capability / Rule。
在 Spec 或等价业务契约未达到准入条件前，不进入正式实现。

### 3.3 继续开发已有 REQ
读取：
`AGENTS.md` → `docs/project/requirements/v1/18_最终决策表.md` → `docs/project/requirements/v1/16_Codex开发任务书.md` → 当前阶段专题需求 → `progress-log` / Evidence → 最近变更 → 本次任务所需 Capability / Rule。
只读取与当前任务相关的专项资产，不默认扫描全部 `doc/`。

### 3.4 Bug / 缺陷修复
先确认缺陷相对于哪个事实源成立：
`Spec / Acceptance / Baseline` → 当前行为证据 → 复现信息 → 修复范围。
若期望行为本身未定义，应标记为 `TBD / Open Item`，而不是由 AI 自行认定“这一定是 bug”。

### 3.5 需求 / 架构 / 契约变更
先建立变更草案，识别受影响对象（REQ、Architecture、Module、API、Data、UI、Tests、Release），确认后再执行。

### 3.6 Code Review / 验收 / 发布
以 Evidence 为中心：检查对应门禁是否有证据，避免把“已经写过文档”当作“已经完成”。

## 3.7 通用手册路由

`doc/handbook` 是公司通用专业手册库，负责提供某类工作的标准方法、结构和文档编制要求。

通用手册不是项目事实源，也不是全局强制 Rule；AI 应根据当前任务和 Workflow 按需读取。

### 技术架构选型

当当前工作进入技术架构设计、技术方案评审或技术选型文档编制时：

1. 先读取当前项目的需求、项目事实、PROJECT_PROFILE / ASSESSMENT 及已批准决策；
2. 再读取：
   `doc/handbook/architecture/通用技术架构选型说明书_公司版_V3.2.md`
3. 根据实际项目情况生成项目专属技术架构文档；
4. 不得机械复制模板中的占位符、示例或默认技术方案；
5. 尚未确定的技术选择必须明确记录为待决事项，不得自行假设为最终结论；
6. 通用手册本身不得因单个项目的使用而被修改。

### 项目开发计划

当需求范围和主要技术方案已经明确，进入开发排期、任务拆分或工时估算时：

1. 先读取当前项目已批准的需求和技术架构；
2. 再读取：
   `doc/handbook/project-management/通用项目开发计划_公司版_V3.2.md`
3. 根据项目实际情况确定阶段、功能、任务、依赖、工时、里程碑和交付物；
4. 不得把模板中的固定阶段、固定工期或示例任务直接视为项目事实；
5. 项目计划必须反映当前已批准的项目范围和技术方案；
6. 通用手册本身不得因单个项目的使用而被修改。

### 手册使用原则

- `handbook` 负责“这类工作应该怎么编制”；
- `governance` 负责“AI 按什么流程执行”；
- `rules` 负责“具体执行必须遵守什么约束”；
- `capabilities` 负责“完成专项工作的专业方法”；
- 项目文档负责记录“当前项目最终采用什么”。

AI 不得因为发现 `doc/handbook` 中存在某项标准，就自动将其升级为当前项目的强制技术方案；项目最终方案仍以项目事实、已批准决策和当前 Workflow 为准。

## 4. Workflow / Stage / Capability / Rule 边界

- `PROJECT_PROFILE`：项目是什么。
- `ASSESSMENT`：项目当前风险、约束与治理需求如何。
- `WORKFLOW`：这个项目采用哪条执行路径。
- `STAGE`：当前处于生命周期哪个阶段。
- `CAPABILITY`：当前需要哪类专项能力。
- `RULE`：专项能力中的具体约束。
- `EVIDENCE`：完成的证据。
- `TRACE`：对象之间如何关联。

禁止把 Capability 直接写成全局强制规则，也禁止把某个具体项目的事实反向沉淀成通用 Rule。

## 5. 默认规则与按需规则

### 默认启用
- 范围不越界；
- 不伪造事实；
- 不泄露密钥；
- 变更可追踪；
- 关键判断可回溯；
- 完成状态必须有证据；
- 代码修改遵守当前已批准的项目技术约束。

### 按需启用
- Vue / React / TypeScript / Java / Spring 等技术栈规则；
- REST / GraphQL / RPC 等 API 能力；
- 自动化、性能、安全测试；
- UI 设计稿、参考站、Tailwind 等视觉能力；
- 小程序 / 原生 App / 桌面端 / 后台等平台能力。

## 6. 规则解析顺序

当多个规则同时生效：

`项目事实 → 项目决策 → 当前 Workflow → 当前 Capability → 通用 Rule → 个人偏好`

个人偏好不能覆盖项目事实和已批准决策。

若两条已激活 Rule 冲突，AI 必须指出冲突来源并按冲突治理流程处理，不得偷偷选择一条。

## 7. AI 工作权限

### 可直接执行
- 读取与分析项目文件；
- 运行低风险构建、静态检查、测试；
- 生成草稿、检查表、分析结果；
- 在已确认范围内进行局部代码修改。

### 需要确认
- 改变业务范围；
- 改变数据库结构、接口契约或核心架构；
- 删除或大规模移动文件；
- 修改生产配置、真实凭据或真实数据；
- 引入新的外部依赖；
- 发生高影响 Workflow / Capability 变化；
- 无法从现有事实确定正确答案时做出不可逆决定。

## 8. 写入边界

### 可以写入
- 当前 REQ 对应文档；
- 当前 Workflow 允许的代码范围；
- 经过治理确认的 Change / Decision / Evidence；
- 测试结果、验证记录、交接信息。

### 不应直接写入通用库
- 单项目具体路径；
- 单项目真实 API 地址、密钥、账号；
- 单项目视觉风格；
- 单项目临时 workaround；
- 尚未验证的经验性结论。

## 9. 变更治理

任何以下变化都应进入 `WORKFLOW_CHANGE / REBASE` 或对应变更机制：

- 项目 Profile 发生变化；
- Workflow 路径发生变化；
- 新增 / 移除关键 Capability；
- 已批准 Spec / Architecture / API / Data 发生变化。

变更后的对象状态必须明确为：
`KEEP / REVALIDATE / INVALIDATE / ADD / REMOVE`。

## 10. Evidence 与 Progress

项目进度遵循：

> **No Evidence, No Progress Claim**

至少区分：

- 代码完成；
- 测试完成；
- 验收完成；
- 合并完成；
- 发布完成。

“代码已经写好”只能证明代码产出，不能自动证明其他状态。

今后每个开发批次的 Git commit message 必须使用中文，一句话总结本批任务目的；完成推送后的交付说明也用中文，列出分支、提交及远端 SHA。

近期交付顺序是 Demo 首先完成，完整 V1 以后精进。求职 Demo 优先打通文件整理/导入、建库、带来源问答、最小学习陪练和重启恢复；默认演示保持无费用 Mock。真实 DeepSeek 只有在设置中显式选择、完成本地同意和系统凭据、并由用户主动确认费用估算后才允许外发。进度报告分别给出 Demo 可用性与完整 V1 阶段状态。

## 11. 安全红线

- 禁止把真实密钥、Token、密码、私钥、生产凭据写入源码、Prompt、普通文档或 Git；
- 不得在未授权情况下操作真实支付、生产数据库、生产用户数据；
- 不得为了绕过权限、审核或安全控制而修改事实源；
- 不得把“看起来合理”的敏感数据直接复制到示例中。

## 12. 本文件的维护

修改 `AGENTS.md` 本身属于治理级变更。修改后至少检查：

- 启动路由是否变化；
- 事实源层级是否变化；
- Rule / Capability 边界是否变化；
- Workflow 是否需要 Rebase；
- 是否存在旧路径、旧术语或项目绑定残留。
